{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch C++ Active File",
            "type": "cppdbg",
            "request": "launch",
            "preLaunchTask": "C/C++: g++ build active file",
            "program": "${fileDirname}/${fileBasenameNoExtension}.exe",
            "args": [],
            "cwd": "${fileDirname}",
            "stopAtEntry": false,
            "externalConsole": false,
            "MIMode": "gdb"
        }
    ]
}
